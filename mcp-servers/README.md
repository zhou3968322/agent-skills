# MCP Servers

本目录包含 Model Context Protocol (MCP) 服务定义，使 Agent 能够发现并调用 `agent-skills` 中的工具能力。

## 集成方式

将 `server-config.json` 注册到支持 MCP 的客户端（如 Claude Desktop）即可使用。

## 可用工具

- `analyze_complexity` - 代码复杂度分析
